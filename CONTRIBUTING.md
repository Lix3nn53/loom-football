# Contributing

Internal project. Open an issue or PR if you have access. See [WORKSPACE.md](./WORKSPACE.md) for a quick map of the codebase.

## Local setup

```bash
npm install
npm run dev    # http://localhost:3001
```

## Before opening a PR

```bash
npm run lint
npm run format
npm run build
```
